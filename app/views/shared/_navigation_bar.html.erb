<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Second-Hand Books', root_path, class: 'navbar-brand' %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Nosotros</a>
        </li>
      </ul>
      <div class="d-flex">
        <p class="navbar-text float-right">
          <% if user_signed_in? %>
            <span><%= current_user.email %></span> |
            <%= link_to 'Mi Perfil',  profile_path(current_user), class: 'btn btn-outline-primary' %> |
            <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: 'btn btn-outline-secondary' %>
          <% else %>
            <%= link_to "Registrarse", new_user_registration_path, :class => 'btn btn-outline-secondary' %> |
            <%= link_to "Iniciar Sesión", new_user_session_path, :class => 'btn btn-outline-secondary' %>
          <% end %>
        </p>
      </div>
  </div>
</nav>